<!DOCTYPE html>
<html lang = en>
<head>
  <title>Il arrabiato</title>
</head>

<body>
<div class="masthead">
  <h1>Il Arrabiato</h1>
  <div class="topnav">
    <input type="text" placeholder="Search..">
    <%= link_to "Log Out", destroy_user_session_path, :method => 'delete'%>
    <img src= "/project/default-user1.png" alt="generic-user"> <!-- Don't know why is not working -->
  </div>
</div>

<div id="user_container">
  <%= current_user.name %>
  <%= link_to 'Reclaim!', new_post_path%>

</div>

<div id ="wall_options">
  <div class="options">
    <div>
      <%= link_to 'Your Posts', new_post_path%>
    </div>
    <div>
      <%= link_to 'Your Profile', profiles_path%>
    </div>
    <div>
      <%= link_to 'Dumpster', dumpsters_path%>
    </div>
    <div>
      <%= link_to 'Blacklist', blacklists_path%>
    </div>
  </div>
</div>


<table id = "post_container2">
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Description</th>
      <th>Open</th>
      <th>Location</th>
      <th>Solved</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
    <tr>
      <td><%= link_to post.user.name, post.user %></td>
      <td><%= post.user.name %></td>
      <td><%= post.title %></td>
      <td><%= post.description %></td>
      <td><%= post.open %></td>
      <td><%= post.location %></td>
      <td><%= post.solved %></td>
      <td><%= post.date %></td>

      <td><%= link_to 'Comment', new_comment_path(post: post)%></td>

      <!--if the user is logged in-->
      <td><%= link_to 'Show Post', post %></td>

        <thead>
          <tr>
            <th>User</th>
            <th>Comment</th>
            <th colspan="2"></th>
          </tr>
        </thead>
        <tbody>
          <% @comments.each do |comment| %>
            <tr>
              <td><%= link_to comment.user.name, comment.user %></td>
              <td><%= comment.comment %></td>
            </tr>
          <% end %>
          </tbody>
      </tr>
    <% end %>
    </br>
  </tbody>
</table>






